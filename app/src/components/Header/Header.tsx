import React from 'react';
import cx from 'classnames';

import styles from './Header.module.scss';

const Header = (): JSX.Element => (
    <header className={cx(styles.header, styles.mainHeader)}>

    </header>
);

export default Header;
